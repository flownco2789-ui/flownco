// next.config.js
module.exports = {
  images: {
    domains: ["picsum.photos"],
  },
};

/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
};

module.exports = nextConfig;
